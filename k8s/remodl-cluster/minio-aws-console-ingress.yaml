apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-aws-console
  namespace: minio-aws
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`infra-hub-307.remodl.ai`) && PathPrefix(`/minio/aws`)
      kind: Rule
      services:
        - name: minio-aws-console
          port: 9443
      middlewares:
        - name: minio-aws-strip-prefix
  tls:
    secretName: infra-hub-tls
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: minio-aws-strip-prefix
  namespace: minio-aws
spec:
  stripPrefix:
    prefixes:
      - /minio/aws
---
apiVersion: v1
kind: Secret
metadata:
  name: infra-hub-tls
  namespace: minio-aws
type: kubernetes.io/tls
data:
  tls.crt: ""
  tls.key: ""
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: infra-hub-tls
  namespace: minio-aws
spec:
  secretName: infra-hub-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - infra-hub-307.remodl.ai

apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler
  labels:
    app: scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler
  template:
    metadata:
      labels:
        app: scheduler
    spec:
      nodeSelector:
        inferx-scheduler: "true"
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      hostPID: true
      containers:
        - name: scheduler
          image: inferx/inferx_platform:${VERSION}
          imagePullPolicy: IfNotPresent
          env:
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP          
          - name: RUN_SERVICE
            value: "Scheduler"
          - name: STATESVC_ADDR
            value: "http://statesvc:1237"
          - name: ETCD_ADDRS
            value: "http://etcd:2379"
          - name: SCHEDULER_PORT
            value: "1238"
          volumeMounts:
            - mountPath: /opt/inferx/log
              name: opt-inferx-log
          command: ["./svc"]
      volumes:
        - name: opt-inferx-log
          hostPath:
            path: /opt/inferx/log         
---
apiVersion: v1
kind: Service
metadata:
  name: scheduler
  labels:
    app: scheduler
spec:
  type: NodePort
  selector:
    app: scheduler
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 31502

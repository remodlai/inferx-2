apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-proxy
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-proxy
  template:
    metadata:
      labels:
        app: mcp-proxy
    spec:
      containers:
      - name: proxy
        image: remodlai/mcp-proxy:latest
        ports:
        - containerPort: 8080
          name: sse
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-proxy
  namespace: default
spec:
  selector:
    app: mcp-proxy
  ports:
  - port: 8080
    targetPort: 8080
    name: sse
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-proxy-ingress
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - mcp.remodl.ai
    secretName: mcp-proxy-tls
  rules:
  - host: mcp.remodl.ai
    http:
      paths:
      - path: /sse
        pathType: Prefix
        backend:
          service:
            name: mcp-proxy
            port:
              number: 8080
      - path: /messages
        pathType: Prefix
        backend:
          service:
            name: mcp-proxy
            port:
              number: 8080
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: mcp-proxy
            port:
              number: 8080
